# Example environment variables for local testing and CI
# Do NOT commit real secrets to the repository. Use GitHub Actions secrets for production.

# ============================================================================
# REQUIRED SECRETS (Sensitive - Add to .env.local for local dev)
# ============================================================================

# Web3Forms access key - Get from: https://web3forms.com/
# Set as GitHub secret: WEB3FORMS_KEY → mapped to VITE_WEB3FORMS_KEY in Actions
VITE_WEB3FORMS_KEY=your_web3forms_access_key_here

# Google Maps API Key - Get from: https://console.cloud.google.com/google/maps-apis
# Used for: 
#   - Live hours fetching (Places API New)
#   - Embedded maps on locations page (Google Maps Embed API)
#   - Future interactive maps (Maps JavaScript API)
# SECURITY: Restrict key to ONLY these 3 APIs in Google Cloud Console
# Set as GitHub secret: GOOGLE_PLACES_API_KEY → mapped to VITE_GOOGLE_PLACES_API_KEY
VITE_GOOGLE_PLACES_API_KEY=your_google_api_key_here

# ============================================================================
# OPTIONAL CONFIGURATION (Non-sensitive or semi-sensitive)
# ============================================================================

# NRS Plus ordering platform URL
# Example: https://order.nrsplus.com/romamart
VITE_NRS_URL=

# Google Tag Manager container ID
# Example: GTM-XXXXXXX
VITE_GTM_ID=

# Featurable reviews carousel ID (optional)
# Get from: https://featurable.com/
VITE_FEATURABLE_KEY=

# Cache duration for Google Places API responses (in milliseconds)
# Default: 3600000 (1 hour)
# Increase to reduce API calls, decrease for more frequent updates
VITE_PLACES_CACHE_DURATION=3600000

# ============================================================================
# INSTRUCTIONS
# ============================================================================
# 1. Copy this file to .env.local (git ignored)
# 2. Replace placeholder values with your actual keys
# 3. For production: Add secrets to GitHub → Settings → Secrets and variables → Actions
# 4. Never commit .env.local or real secrets to the repository
# 5. GitHub Actions workflow automatically maps secrets at build time
